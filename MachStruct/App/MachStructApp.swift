import SwiftUI

@main
struct MachStructApp: App {
    var body: some Scene {
        WindowGroup("MachStruct") {
            ContentView()
        }
        .defaultSize(width: 960, height: 640)
    }
}
